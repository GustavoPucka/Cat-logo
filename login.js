// Função para exibir a mensagem de login com sucesso
function showLoginSuccessMessage() {
    const loginSuccessMessage = document.getElementById("login-success-message");
    loginSuccessMessage.style.display = "block";
  }

  // Chamando a função para exibir a mensagem (você pode chamar isso após o login ser bem-sucedido)
  showLoginSuccessMessage();